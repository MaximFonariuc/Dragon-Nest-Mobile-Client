资源加载中的重要抽象概念
1.资源分成两种：程序资源（ProgramAsset）和一般资源（Asset）。其中程序资源指Pandora模块运行需要的资源，配置在管理后台规则中。一般资源是指程序运行时动态加载的资源，配置在管理后台活动配置中。
2.资源分成两步：第一步加载到本地目录，第二步加载到内存。

资源的使用分两种情况
1.加载后并在内存对象池中缓存一份，不需要使用资源的时候需要手动释放一次引用，当引用计数为0的时候，资源进入可回收状态。
2.加载后即使用，不维护额外的引用计数，当资源不再使用的时候，进入可回收状态。